const boom = (n) =>  {
  for (let i = 0; i < 3; i++) {
    bam(n);
  }
  
  for (let k = 0; k < 10000; k++) {
    console.log(k);
  }
};

const bam = (n) => {
  for (let j = 0; j < m; j++) {
    console.log(j);
  }
};
